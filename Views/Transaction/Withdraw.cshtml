@model Transaction
<h1>Welcome</h1>
<p>Current balance: $@ViewBag.UserAccount.Balance</p>
<form asp-action="Subtract" asp-controller="Transaction" method="post">
    <label asp-for="Amount">How much would you like to Withdraw?</label> 
    <span asp-validation-for="Amount" class="text text-danger"></span>
    <input asp-for="Amount">
    <input type="hidden" asp-for="UserId">
    <input type="submit" value="Submit">
</form>

@{
    string message = TempData["Success"] as string;
    if (message != null)
    {
        <div class="text-success">@message</div>
    }
}

<hr>
<table class="table table-hover">
    <thead>
        <tr>
            <th>Amount</th>
            <th>Date</th>
        </tr>
    </thead>
    <tbody>
    @foreach(var t in @ViewBag.UserAccount.Transactions)
    {
        <tr>
            <td>@t.Amount</td>
            <td>@t.CreatedAt</td>
        </tr>
    }
    </tbody>
</table>

<script>
    function idleLogout() {
        var t;
        window.onload = resetTimer;
        window.onmousemove = resetTimer;
        window.onmousedown = resetTimer;     
        window.ontouchstart = resetTimer;     
        window.ontouchmove = resetTimer;  
        window.onclick = resetTimer;     
        window.onkeydown = resetTimer;
        window.addEventListener('scroll', resetTimer, true); 

        function logOut() {
            alert("Your session has timed out due to inactivity. You will now be redirected to login page.");
            @{
                Context.Session.Clear();
            }
            window.location.href=("/login");
        }

        function resetTimer() {
            clearTimeout(t);
            t = setTimeout(yourFunction, 10000);  // time is in milliseconds
        }
    }
    idleLogout();
</script>